# Stripe (test mode — use while testing checkout)
STRIPE_SECRET_KEY_TEST=
STRIPE_PUBLISHABLE_KEY_TEST=
STRIPE_PRICE_ID_TEST=

# Stripe (live mode — swap these in when going live)
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=

# Public site URL (optional locally — defaults to http://localhost:3000 or VERCEL_URL on deploy)
NEXT_PUBLIC_BASE_URL=

# Direct Vercel Blob URL for the book PDF (upload via Vercel dashboard, paste the public URL here)
BLOB_PDF_URL=
